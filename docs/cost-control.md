# Cost control

Prepare exact inputs; never use Astra for file search or infrastructure repair. Keep history minimal, avoid repeated MCP discovery, separate art from integration, count visual iterations as work, and use a convergence pass. Usage varies by complexity, reasoning, tools, and iteration count.
