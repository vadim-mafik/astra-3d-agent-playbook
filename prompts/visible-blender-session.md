# Visible Blender session

Use this when you want Astra's changes to appear in the Blender window that is already open.

```text
Continue the assigned task through Blender MCP using the currently open Blender instance.

Do not launch a separate headless or background Blender process.
Do not switch to Computer Use unless a required action has no Blender MCP or deterministic Blender/Python route.

Keep changes in the current visible scene.

Python inside the current Blender session is allowed for deterministic operations when it is the shortest tool for the job.

Continue the normal model -> inspect -> critique -> revision loop.
```
